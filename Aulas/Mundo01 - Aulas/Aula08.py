'''
Math
  ceil (Arredonda pra cima)
  floor (Arredonda pra baixo)
  trunc
  pow
  sqrt
  factorial
import(math)
from(math)import(sqrt)

#import math
from math import sqrt, floor
num = int(input('Digite um número: '))
raiz = sqrt(num)
print('A raiz de {} é igual à {:.2f}'.format(num, floor(raiz)))
'''

